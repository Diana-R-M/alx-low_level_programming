#include "main.h"
/**
 * main - Entry point
 * Description: 'Checks if a random number is positive, zero or negative.'
 * Return: Always 0 (Success)
 */
void print_alphabet_x10(void)
{
	char alphabet;
	int alphabet10x;

	for (alphabet10x = 1; alphabet10x <= 10; i++)
	{
		for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
		putchar(alphabet);

		putchar('\n');

		return (0);
	}
}
